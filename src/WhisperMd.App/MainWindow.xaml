<Window x:Class="WhisperMd.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WhisperMd"
        Width="1220"
        Height="780"
        MinWidth="980"
        MinHeight="640"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource AppBackgroundBrush}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="232" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource SidebarBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Margin="22,24,18,24">
                    <TextBlock Text="WhisperMd"
                               Foreground="White"
                               FontFamily="Segoe UI Semibold"
                               FontSize="22" />
                    <TextBlock Text="локальная транскрибация"
                               Margin="0,4,0,0"
                               Foreground="{StaticResource SidebarMutedBrush}"
                               FontSize="12" />
                </StackPanel>

                <StackPanel Grid.Row="1">
                    <Button Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateCommand}"
                            CommandParameter="transcription"
                            Content="Транскрибация" />
                    <Button Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateCommand}"
                            CommandParameter="history"
                            Content="История" />
                    <Button Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateCommand}"
                            CommandParameter="settings"
                            Content="Настройки" />
                </StackPanel>

                <Border Grid.Row="3"
                        Margin="18,12,18,20"
                        Padding="12"
                        Background="#20242C"
                        CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Whisper small"
                                   Foreground="White"
                                   FontFamily="Segoe UI Semibold"
                                   FontSize="12" />
                        <TextBlock Text="{Binding BackendSummary}"
                                   Margin="0,3,0,0"
                                   Foreground="{StaticResource SidebarMutedBrush}"
                                   FontSize="11" />
                        <TextBlock Text="{Binding LanguageSummary}"
                                   Margin="0,2,0,0"
                                   Foreground="{StaticResource SidebarMutedBrush}"
                                   FontSize="11" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Border Grid.Column="1" Background="{StaticResource AppBackgroundBrush}">
            <ContentControl Content="{Binding CurrentPage}" />
        </Border>
    </Grid>
</Window>
